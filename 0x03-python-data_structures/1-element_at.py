#!/usr/bin/python3
def element_at(my_list, idx):
    i = len(my_list)
    for y in idx:
        if (idx < 0):
            return none
        elif (idx > i):
            return none
        else:
            continue
